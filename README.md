# tourism
* Installation :

   ```$ expo init client```
   
   ```$ cd client```
   
   ```$ npm start```
   
   Open a new terminal and run the following commands to compile and launch your application
   
   ```$ npm run android```
